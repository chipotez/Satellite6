---
- name: "Include install vars"
  include_vars: "vars/1_install.yml"
  tasks:
      - name: Actualizando Sistema Operativo
        yum:
          name: '*'
          state: latest


#Install the base software
      - name: "Install_software"
        become: "yes"
        yum:
          name: "{{ item }}"
          state: "latest"
          with_items: "{{ satellite }}"
